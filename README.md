#A NodeJS project with Passport.js and JWT

We used passport for authentication
- Sign Up
- Login

After user logins successfully, the system will send back JWT,
and user will use the JWT to access secured route.